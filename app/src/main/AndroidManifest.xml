<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Camera and Audio permissions for AR and video recording -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Foreground Service permissions -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />

    <!-- Haptic reminders during capture -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Overlay permission for capture button -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- ARCore requires camera -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />
    
    <!-- ARCore support -->
    <uses-feature
        android:name="android.hardware.camera.ar"
        android:required="false" />

    <!-- Internet for ML Kit models (optional) -->
    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- External storage for import/export -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" 
        tools:ignore="ScopedStorage" />

    <!-- Android 13+ granular media permissions -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- Contacts permission for phone share feature -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <!-- NFC for team invite sharing -->
    <uses-permission android:name="android.permission.NFC" />
    <uses-feature android:name="android.hardware.nfc" android:required="false" />

    <queries>
        <package android:name="com.teamsnap.teamsnap" />
        <package android:name="com.teamsnap.mobile" />
        <package android:name="com.teamsnap.android" />
        <package android:name="com.teamsnap" />
        <package android:name="com.sportsengine.mobile" />
        <package android:name="com.maxpreps.android" />
        <package android:name="com.gc.android.team" />
        <package android:name="com.teamlinkt.app" />
        <package android:name="com.jerseywatch" />
        <package android:name="com.bluelabelapps.sportsyou" />
        <package android:name="com.leagueapps.mobile" />
        <package android:name="com.stack.team" />
        <package android:name="com.hudl.android" />
        <package android:name="com.teamreach.app" />
        <package android:name="com.rosterbot.app" />
        <package android:name="com.crossover.app" />
        <package android:name="com.leaguelineup.mobile" />
    </queries>

    <application
        android:name=".PlayerIDApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.PlayerID"
        tools:targetApi="31">
        
        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.PlayerID"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="playerid-teamsnap"
                    android:host="oauth" />
            </intent-filter>
        </activity>

        <!-- Background Assistant Service -->
        <service
            android:name=".service.SpotrAssistantService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <service
            android:name=".capture.ScreenCaptureService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaProjection" />

        <!-- ARCore metadata -->
        <meta-data
            android:name="com.google.ar.core"
            android:value="optional" />

        <!-- ML Kit metadata -->
        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="ocr" />

        <!-- Remove transitive AndroidX Startup provider; it crashes on launch on device builds. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="remove" />

    </application>

</manifest>