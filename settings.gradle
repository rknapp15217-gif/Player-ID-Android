pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_PROJECT)
    repositories {
        google()
        mavenCentral()
        maven { url = 'https://www.jitpack.io' }
        maven { url = 'https://dl.bintray.com/arthenica/maven' }
    }
}

rootProject.name = "PlayerID"
include ':app'
include ':shared'